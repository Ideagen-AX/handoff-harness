node_modules
.next
public/captures
public/library
.env.local
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts

# Content-hash cache + job checkpoints for the large-prototype pipeline
.harness-cache

# The slide exporter uses one lightweight format: specs/templates/blank1-bg.png
# (a pre-composited Blanks-Blank-1 background, committed). Any stray .pptx masters
# left over from the old approach stay local — they're no longer used or needed.
specs/templates/*.pptx
specs/templates/*.potx

# Harness runtime job cache
.harness-cache

# macOS
.DS_Store
