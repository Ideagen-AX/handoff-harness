// Single source of truth for the app's semantic version. Bump on each meaningful
// release; every saved run is stamped with the version that produced it, so the
// library can show (and compare) which version of the tool generated each run.
// Keep in sync with package.json "version".
export const APP_VERSION = "0.3.0";
