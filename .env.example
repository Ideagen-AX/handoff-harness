# The harness talks to models through the Vercel AI Gateway.
#
# Local development: get a key from the Vercel dashboard (AI Gateway → API Keys)
# and paste it here in a file named `.env.local`.
#
# On Vercel: you don't need this — deployed functions authenticate to the
# gateway automatically via OIDC.
AI_GATEWAY_API_KEY=
